{
  "name": "cafe-delicious",
  "version": "1.0.0",
  "description": "Кафе заказ берүү системасы",
  "scripts": {
    "dev": "concurrently \"cd backend && uvicorn server:app --host 0.0.0.0 --port 8001 --reload\" \"cd frontend && yarn start\"",
    "build": "cd frontend && yarn build",
    "start": "cd backend && uvicorn server:app --host 0.0.0.0 --port 8001"
  },
  "keywords": ["cafe", "ordering", "react", "fastapi", "mongodb", "kyrgyz"],
  "author": "Cafe Delicious Team",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "python": ">=3.8.0"
  }
}